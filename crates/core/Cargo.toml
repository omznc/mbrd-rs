[package]
name = "mbrd-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The mbrd board model and the .mbrd file format, with no UI attached."

[dependencies]
anyhow.workspace = true
flate2.workspace = true
pulldown-cmark.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
tar.workspace = true
zip.workspace = true

# WASM EXPERIMENT: std's clock panics on wasm32-unknown-unknown.
[target.'cfg(target_family = "wasm")'.dependencies]
web-time = "1"
